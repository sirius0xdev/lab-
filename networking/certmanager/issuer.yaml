    apiVersion: cert-manager.io/v1
    kind: ClusterIssuer
    metadata:
      name: letsencrypt-dev
      annotations:
          # This annotation is required for cert-manager to work with hostNetwork
        acme.cert-manager.io/http01-edit-in-place: "true"
    spec:
      acme:
        server: https://acme-v02.api.letsencrypt.org/directory
        email: sirius-sec@protonmail.com
        privateKeySecretRef:
          name: letsencrypt-dev-account-key
        solvers:
        - http01:
            ingress:
              ingressClassName: cilium # Or specify an ingressClassName if using Ingress
              serviceType: ClusterIP

